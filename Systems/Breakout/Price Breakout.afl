#include <Header.afl>

// Variables
HighPeriods = Optimize("High Periods", 120, 20, 300, 10);
LowPeriods = Optimize("Low Periods", 10, 10, 200, 10);

// Signal
Buy = Cross(Price, Ref(HHV(Price,HighPeriods),-1));
Sell = Cross(Ref(LLV(Price,LowPeriods),-1), Price);
Exclude = HighPeriods - LowPeriods < 10;

#include <Footer.afl>