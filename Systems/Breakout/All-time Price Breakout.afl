#include <Header.afl>

// Variables
LowPeriods = Optimize("Low Periods", 20, 10, 300, 10);

// Signal
Buy = Cross(Price, Ref(HHV(Price,99999),-1));
Sell = Cross(Ref(LLV(Price,LowPeriods),-1), Price);

#include <Footer.afl>